[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200

[profile.default.rpc_endpoints]
besu = "${BESU_RPC_URL:-http://localhost:8545}"

[fmt]
line_length = 120
tab_width = 4
bracket_spacing = true
